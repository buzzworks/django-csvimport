#!/usr/bin/make -f

# Uncomment this to turn on verbose mode.
#export DH_VERBOSE=1

%:
	dh $@ --with python2 --buildsystem=python_distutils

override_dh_install:
	dh_install
	
	# Fix permissions
	find debian/*/usr/lib/python*/dist-packages -type f -exec chmod -x {} \;
